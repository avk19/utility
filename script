function compareIgnoreCase(value1, value2) {
  // Convert null, undefined, and blank values to an empty string for comparison
  value1 = value1 ? value1.toString().toLowerCase() : "";
  value2 = value2 ? value2.toString().toLowerCase() : "";

  return value1 === value2;
}

